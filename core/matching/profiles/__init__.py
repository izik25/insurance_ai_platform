"""Per-category matching configuration (feature weights, hard constraints,
thresholds) - versioned YAML under core/matching/profiles/data/, calibrated
from the existing corpus (see scripts/calibrate_matching.py) rather than
hardcoded in the matching engine.
"""
