# איך המערכת עובדת — הסבר פשוט

מסמך זה מסביר בשפה פשוטה מה המערכת עושה ואיך. ליד כל שלב יש בסוגריים את שם הרכיב הטכני שמבצע אותו, כדי שגם מפתח שמצטרף לפרויקט ידע מיד לאיזה קוד להסתכל. לפירוט המלא — ראו [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) (מלא) או [OVERVIEW_TECH_SIMPLE.md](OVERVIEW_TECH_SIMPLE.md) (תמצית טכנית).

## מה המערכת עושה, במשפט אחד

המערכת אוספת מסמכי פוליסה מאתרי חברות ביטוח שונות, "קוראת" אותם, ומזהה **אילו נספחים אצל חברות שונות מתארים בעצם את אותו כיסוי ביטוחי** — כדי שאפשר יהיה להשוות ביניהם.

**למה זה בכלל קשה?** כל חברת ביטוח קוראת לכיסויים שלה בשמות ומספרים שונים. "נספח 12" אצל מגדל יכול להיות "כיסוי 47" אצל הפניקס — אותו דבר בפועל, מספור שונה לגמרי. המערכת לומדת להכיר את זה לפי **תוכן** הכיסוי, לא לפי המספר או השם שלו.

## התהליך, שלב אחרי שלב

```
אתר חברת ביטוח → הורדת קבצים → קריאת המסמך → זיהוי מידע (LLM) → השוואה (embeddings) → דשבורד
   Downloader        Downloader        Parser/OCR         Extractor          Matching        API+Frontend
```

### שלב 1: איסוף המסמכים — `BaseDownloader`, תיקיית `companies/<חברה>/downloader.py`
לכל חברת ביטוח יש אתר משלה, בנוי אחרת, ולעיתים עם הגנות נגד רובוטים (כמו CAPTCHA / WAF). לכן לכל חברה יש "פלאגין" נפרד עם קוד הורדה משלה — חלק ניגשים ישר ל-API (`httpx`), חלק דורשים דפדפן אמיתי (`Playwright`) כדי לעקוף את ההגנה. הפלט: קבצי PDF שיורדים לתיקיית `data/raw_documents/<חברה>/`.

### שלב 2: קריאת המסמך — `BaseParser` + `core/ocr/`
כל PDF עובר קריאה (`PyMuPDF`) כדי לחלץ ממנו טקסט. אם הטקסט לא "מוטבע" בקובץ (למשל מסמך סרוק כתמונה) — המערכת נופלת ל-**OCR** (`Tesseract`, זיהוי תווים אופטי) כדי לקרוא אותו כמו שרואים בעין.

### שלב 3: זיהוי מידע מובנה (AI) — `BaseExtractor` + `core/extraction/llm_extract.py`
מודל שפה (**LLM**, בפועל OpenAI `gpt-4.1-mini`) קורא את תוכן המסמך ומחלץ ממנו שדות אחידים: איזה סוג כיסוי זה, תנאי זכאות, סכומי ביטוח, תקופת המתנה, חריגים, טווח גילאים ועוד. כך כל מסמך — מכל חברה — מתורגם לאותה "שפה" משותפת (נשמר בטבלת `document_extractions`). הרצה: `scripts/extract_documents.py` (תמיד עם `--limit` לבדיקות).

### שלב 4: השוואה חכמה בין חברות — `core/embeddings/` + `core/matching/`
המידע שחולץ הופך למעין "טביעת אצבע" מספרית (**embedding**, מודל מקומי `multilingual-e5-large`) שמייצגת את המשמעות של הכיסוי. שני מסמכים מחברות שונות שהמשמעות שלהם דומה — מקבלים ציון דמיון גבוה (**cosine similarity**). מעל 95% המערכת מאשרת התאמה אוטומטית (`auto_confirmed`), מתחת לסף היא מסמנת "לבדיקה ידנית" (`pending_review`). הרצה: `scripts/embed_documents.py` ואז `scripts/match_documents.py`.

### שלב 5: דשבורד לצפייה — `api/` (FastAPI) + `frontend/` (React)
מסך ניהול פשוט שמראה: אילו קבצים ירדו, מה חולץ מכל אחד, ואילו התאמות בין חברות נמצאו — עם אחוז הדמיון. קריאה בלבד, בלי קריאות ל-LLM. הרצה מקומית: `uvicorn main:app` (backend) + `npm run dev` בתיקיית `frontend/`.

## המצב היום

| חברה | מסמכים במערכת |
|---|---|
| מגדל | 1,053 |
| הפניקס | 1,903 |
| כלל | 527 |
| מנורה מבטחים | 777 |
| ביטוח ישיר | 231 |

כל חברה נבנתה בנפרד, ואפשר להוסיף חברה חדשה בלי לגעת בקוד של החברות הקיימות.

## מה עוד נשאר

1. להוסיף חברות ביטוח נוספות.
2. לעבור ידנית על מדגם מההתאמות ולוודא שהן הגיוניות.
3. לחזק את העמידות של תהליך החילוץ מול קריסות (checkpointing).
4. לפתוח את מסך "השוואה" בדשבורד (כרגע קיים אבל לא פעיל).
